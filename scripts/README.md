# scripts/

Legacy REST helpers retained for compatibility and maintenance only. Agent skills use unified host MCP `topview-mcp` (`mcp.example.json` / `~/.cursor/mcp.json`) and must not execute these scripts.
